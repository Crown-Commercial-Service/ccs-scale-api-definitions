components:
  parameters:
  
    BasketParam:
      name: basketID
      in: path
      required: true
      description: Basket ID
      schema:
        $ref: 'https://raw.githubusercontent.com/Crown-Commercial-Service/ccs-scale-api-definitions/CAT_initial/bat/bat-Schema.yaml#/components/schemas/BasketID'
 
    ProductParam:
      name: prodID
      in: path
      required: true
      description: Product ID
      schema:
        $ref: 'https://raw.githubusercontent.com/Crown-Commercial-Service/ccs-scale-api-definitions/master/common/CCS_Standard_API_Schema.yaml#/components/schemas/GenericID'
        
    SupplierItemParam:
      name: supplierItemID
      in: path
      required: true
      description: Catalogue Supplier Item ID
      schema:
        $ref: 'https://raw.githubusercontent.com/Crown-Commercial-Service/ccs-scale-api-definitions/master/common/CCS_Standard_API_Schema.yaml#/components/schemas/GenericID'
        
    OrderScopeParam:
      name: scope
      in: query
      description: For lists of Orders controls the scope of the request - for the User, Organisation or BuyerGroup
      schema: 
        $ref: 'https://raw.githubusercontent.com/Crown-Commercial-Service/ccs-scale-api-definitions/CAT_initial/bat/bat-schema.yaml#/components/schemas/ShareScope'